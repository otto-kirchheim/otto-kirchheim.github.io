/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
	tabWidth: 1,
	useTabs: true,
	bracketSpacing: true,
	printWidth: 120,
	semi: true,
	arrowParens: "avoid",
	endOfLine: "auto",
};

export default config;
